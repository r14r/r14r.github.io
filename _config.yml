# Site
name: "Ralph Göstenmeier"
description: "Entwickler, Architekt, Open Source Enthusiast"
baseurl: "/"

include: ["_pages"]
#permalink: /:title/

# Syntax
markdown: kramdown
highlighter: rouge

# Exclude metadata and development time dependencies (like Grunt plugins)
exclude:
    [
        README.markdown,
        package.json,
        grunt.js,
        Gruntfile.js,
        Gruntfile.coffee,
        node_modules,
    ]

#
sass:
    sass_dir: /assets/css
    style: compressed
